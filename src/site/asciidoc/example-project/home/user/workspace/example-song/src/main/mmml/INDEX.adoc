This directory will be an entry of `JF_PATH` environment variable.

[source, json]
----
{
  "$settings": {},
  "$song": {
    "$includes": ["song/__index__.json"]
  },
  "$parts": "eval:object:...",
  "$patterns": "eval:object:...",
  "$grooves": "eval:object:$(ref .\"$song\".\"$grooves\")",
  "$sequence": "eval:array:$(compose_sequence_array )"
}
----